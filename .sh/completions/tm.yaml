tm:
  - start:
      - --switch:
          - $(tmux list-sessions -F \#S)
      - --aux
  - detach:
      - --help
  - editor:
      - --help
  - opencode:
      - --help
  - quads:
      - --help
  - git:
      - --help
  - kill:
      - $(tmux list-sessions -F \#S)
      - --all
